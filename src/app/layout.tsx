// Root layout — next-intl middleware handles locale routing.
// The [locale] layout manages actual HTML structure.
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
